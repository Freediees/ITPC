import React from 'react'
import AccountPage from '../components/organism/AccountPage'

const AccountScreen = ({ navigation }) => {
    return <AccountPage navigation={navigation} />
}

export default AccountScreen